version: 0.0
os: windows
files:
  - source: target/ussd.jar
    destination: C:\deployments\ussd\
hooks:
  AfterInstall:
    - location: scripts\deploy.ps1
      timeout: 300
      runas: Administrator
